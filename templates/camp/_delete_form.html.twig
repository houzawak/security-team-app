<form method="post" action="{{ path('camp_delete', {'code': camp.code}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ camp.code) }}">
    <button class="btn">Delete</button>
</form>
